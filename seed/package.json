{
  "name": "cosmosdb-seed",
  "version": "1.0.0",
  "description": "CosmosDB seed data loader",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "seed": "node dist/scripts/seed-cosmosdb.js"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "typescript": "^4.0.0",
    "rimraf": "^5.0.0"
  }
}
